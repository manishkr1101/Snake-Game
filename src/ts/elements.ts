const elements = {
    scoreField: document.querySelector('.score h3 span'),
    playBtn: document.getElementById('play'),
    pauseBtn: document.getElementById('pause'),
    canvas: document.getElementById('board')
}

export default elements